# FastLRSearch - requirements.txt
#
# Note: Install PyTorch with CUDA separately using the official selector:
# https://pytorch.org/get-started/locally/
#
# For development, prefer: pip install -e .

# Core ML
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
sentencepiece>=0.1.99  # Required for SigLIP tokenizer

# Vector DB
qdrant-client>=1.7.0

# Image processing
Pillow>=10.0.0
rawpy>=0.19.0
numpy>=1.24.0,<2.0.0  # PySide6 6.4.x requires numpy<2

# Hashing & dedup
imagehash>=4.3.0
xxhash>=3.4.0

# EXIF
exifread>=3.0.0

# UI - pinned to 6.4.x for Fcitx/IME compatibility with system Qt
PySide6>=6.4.0,<6.5.0

# API
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6  # Required for file uploads

# Utilities
tqdm>=4.66.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
